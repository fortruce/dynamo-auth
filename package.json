{
  "name": "dynamo-auth",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "NODE_ENV=${NODE_ENV:-development} node src/index.js | bunyan",
    "test": "NODE_ENV=${NODE_ENV:-test} node test/index.js | faucet",
    "lint": "eslint src/ test/ scripts/",
    "lint:test": "npm run lint; npm run test",
    "test-dev": "watchman-make --make 'npm run' -p '**/*.js' -t lint:test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fortruce/dynamo-auth.git"
  },
  "author": "fortruce",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/fortruce/dynamo-auth/issues"
  },
  "homepage": "https://github.com/fortruce/dynamo-auth#readme",
  "dependencies": {
    "aws-dynamodb": "0.1.49",
    "bunyan": "1.5.1",
    "dotenv": "^1.2.0",
    "restify": "^4.0.3"
  },
  "devDependencies": {
    "eslint": "1.10.3",
    "faucet": "0.0.1",
    "supertest": "1.1.0",
    "tape": "4.4.0"
  }
}
